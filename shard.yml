name: crystal-lua
# description:
authors:
  - Devonte W <https://github.com/devnote-dev>

version: 0.1.0
crystal: '>= 1.9.2'
license: MIT

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    version: ~> 1.5.0

scripts:
  # Geode bug: can't execute cmd script with multiple lines
  postinstall@windows: scripts\install.cmd
